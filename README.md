# Computer-Vision-Engineering-with-PyTorch
This code provides a configurable pipeline for classification and segmentation. It illustrates all the blocks: data, model backbone, modular heads, training loop with amp, validation, checkpoint backup, and inference and explainability functions (Simple Grad-CAM).
